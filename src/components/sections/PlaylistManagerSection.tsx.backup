return (
    <section id="playlist" className="py-20 px-4 bg-slate-50 dark:bg-slate-900">
      <div className="container mx-auto max-w-6xl">
        {/* Page Header */}
        <div className="text-center mb-8">
          <Badge variant="secondary" className="mb-4 bg-purple-500/10 text-purple-500 border-purple-500/20">
            {t('playlistManager.badge')}
          </Badge>
          <h1 className="text-3xl md:text-4xl font-bold mb-4">{t('playlistManager.pageTitle')}</h1>
          <p className="text-lg text-muted-foreground">
            {t('playlistManager.pageSubtitle')}
          </p>
        </div>

        <div className="grid md:grid-cols-3 gap-6">
          {/* ... existing code ... */}
        </div>

        {/* Features Section */}
        <div className="mt-6 p-4 bg-card border rounded-lg">
          <h3 className="font-semibold mb-2">{t('playlistManager.featuresTitle') || 'Features'}:</h3>
          <ul className="text-sm text-muted-foreground space-y-1 grid md:grid-cols-2 gap-2">
            <li>• {t('playlistManager.features.create.title')}</li>
            <li>• {t('playlistManager.features.edit.title')}</li>
            <li>• {t('playlistManager.features.dragDrop.title')}</li>
            <li>• {t('playlistManager.features.import.title')}</li>
            <li>• {t('playlistManager.features.preview.title')}</li>
            <li>• {t('playlistManager.features.share.title')}</li>
          </ul>
        </div>
      </div>
    </section>
  );
